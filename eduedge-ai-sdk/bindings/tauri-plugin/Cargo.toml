[package]
name = "eduedge-ai-tauri-plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
eduedge-ai-core = { path = "../../core" }
serde = { version = "1", features = ["derive"] }
tauri = "2"
tokio = { version = "1", features = ["sync"] }
