# 毕业论文结构设计：融合大语言模型与知识图谱的教学辅助平台

**日期**：2026-04-17  
**论文题目**：融合大语言模型与知识图谱的教学辅助平台设计——以《学术规范和论文写作》为例  
**核心调整**：将主案例由"电磁场（双案例并列）"切换为"学术规范与论文写作（单一主线）"

---

## 核心论点

> 学术写作规范可以被形式化为知识图谱；通过 GraphRAG 将 LLM 的反馈与图谱节点绑定，可以实现"有规范依据、可溯源、可验证"的写作辅助，而非凭空建议。

三个技术贡献：
1. **KG 的设计与构建**——对学术写作规范的形式化建模
2. **GraphRAG 检索绑定机制**——回答强制对齐规范节点
3. **反馈质量的可评测性**——溯源率、采纳率等可量化指标

---

## 章节结构

### 第1章 绪论

**重心**：从"LLM 在写作类课程中的可靠性挑战"出发——幻觉不只是"说错"，更是"无依据地说对"，这在规范性强的学术写作课程中尤其危险。

**研究问题（调整后）**：
- RQ1：如何将学术写作规范形式化为可检索的知识图谱？
- RQ2：如何通过 GraphRAG 将 LLM 反馈绑定到具体规范节点，实现可追溯？
- RQ3：如何评估"规范绑定反馈"相比"自由生成反馈"的质量差异？
- RQ4：如何将薄弱点与知识图谱节点对齐，支撑长期学习画像？

**创新目标**：
1. 提出学术写作规范本体设计方案，覆盖引用格式、段落功能、论证组织等核心维度
2. 实现 GraphRAG 检索与 KG 节点的强绑定机制，支持引用编号溯源
3. 建立三条评测线（KG质量、反馈溯源性、反馈有效性）的可复现评测方案

---

### 第2章 相关技术与理论基础

在现有章节（React/Go/FastAPI/RAG/GraphRAG/LoRA）基础上**新增或扩写**：

**新增内容**：
- **学术写作规范的本体建模研究**：ScholarlyAO、AO（Argument Ontology）、MEO 等已有本体的覆盖范围与局限；HUST 规范的特殊性
- **LLM + KG 的两种集成范式**：KG-to-Text（将 KG 转为文本注入）vs GraphRAG 检索注入（本文路线）的对比与选型依据
- **写作评价维度理论（Rubric）**：作为 KG 节点分类的理论依据；现有 Rubric 框架（如 6+1 Traits、学术写作 Rubric）与本文设计的映射关系

**保留内容**：Transformer、RAG、GraphRAG、工具调用、引导式学习、LoRA/QLoRA

---

### 第3章 学术规范知识图谱设计与构建（新章，核心贡献）

**3.1 领域分析与建模范围**

梳理《学术规范与论文写作》课程中需要形式化的规范维度：
- 引用格式（APA/GB/T 7714 等）
- 章节结构（IMRAD 模型）
- 段落功能分类（背景、方法、结果、讨论）
- 学术语气（hedging、主动/被动语态规范）
- 论证组织（声明—证据—推理链）
- 学术诚信约束（引用完整性、paraphrase 规范）

**3.2 本体设计**

节点类型：
- `规范条款`：具体可引用的规范要求
- `示例片段`：课程讲义中的正例/反例
- `违例模式`：常见错误类型
- `修改建议`：与违例模式对应的修改策略
- `评价维度`：Rubric 中的评分维度

关系类型：
- `违反` (文本片段 → 规范条款)
- `对应修改` (违例模式 → 修改建议)
- `属于` (规范条款 → 评价维度)
- `前置规范` (规范条款 → 规范条款，用于有依赖关系的规范)
- `示例于` (示例片段 → 规范条款)

**3.3 构建流程（B+C 路线）**

- **C路线（基础层）**：以 ScholarlyAO 或 AO 为基础本体，映射到 HUST 学位论文写作规范，保留通用节点，补充 HUST 特有规范节点
- **B路线（扩展层）**：使用 LLM 从课程讲义、规范文档、历年范文中半自动抽取实体与关系；设计抽取提示词（含节点类型约束）；人工审校抽取结果

**3.4 知识图谱质量评估**

- 节点覆盖率：与课程讲义中规范条款的对齐比例
- 关系准确率：人工抽样标注 vs 自动抽取的一致性（Cohen's κ）
- 完整性：是否覆盖课程考核的全部评价维度

---

### 第4章 系统设计与实现

架构保留三层（React + Go + FastAPI），叙事重心转为"KG 如何驱动各模块"。

**4.1 技能系统与对话编排**（保留现有内容，补充写作场景的技能设计）
- `writing_feedback_rag`：结构化写作反馈，强制引用 KG 节点
- `writing_guided`：引导式写作改进，分步推进
- `academic_integrity_check`：学术诚信约束内置于技能提示

**4.2 GraphRAG 检索绑定**（扩写，强调 KG 节点绑定）
- 从 KG 节点出发的图扩展检索：以学生提交文本中识别出的潜在违例为种子，检索相关规范条款及其示例
- 回答格式约束：每条反馈必须携带 KG 节点编号（如 `[REF:APA-3.2]`）
- 知识库更新流程：新增规范文档如何增量更新图索引

**4.3 Rubric 驱动的反馈生成**
- 以 KG 中的`评价维度`节点作为反馈结构的锚点
- 输出格式：维度名称 → 问题定位 → 规范依据（KG节点）→ 修改建议

**4.4 规范检查工具**
- 引用格式检查工具（正则 + 规则引擎，覆盖 APA/GB/T 7714）
- 段落结构检查工具（识别段落功能标记的缺失）
- 工具结果回注到对话上下文

**4.5 学习画像与 KG 节点对齐**
- 薄弱点不再是自由文本，而是与 KG 节点绑定（例："该学生在`引用格式.APA.二次文献`上反复出错"）
- 支持按 KG 维度聚合学生群体画像，辅助教师发现班级共性问题

**4.6 训练与评测管线**（保留，补充写作场景样本类型）
- 新增样本类型：`writing_feedback`（含 KG 节点引用约束）、`norm_check`

---

### 第5章 实验与评估

三条评测线：

| 评测对象 | 核心指标 | 方法 |
|---------|---------|------|
| KG 质量 | 节点覆盖率、关系准确率（Cohen's κ）、维度完整性 | 人工标注对比 |
| 反馈溯源性 | KG引用命中率（反馈中引用的规范节点是否存在且相关） | 自动检查 + 人工抽样 |
| 反馈有效性 | 学生修改采纳率、修改前后 Rubric 得分变化 | 小规模用户实验（10-20人） |

**对比实验设计**：
- 基线 A：纯 LLM 自由生成（无 KG，无检索）
- 基线 B：传统 RAG（向量检索，无图结构）
- 本文方案：GraphRAG + KG 节点绑定

**消融实验**：去掉图扩展（仅向量检索）vs 完整 GraphRAG，对比引用命中率差异。

---

### 第6章 结论

**总结**：
- KG 建模覆盖范围与局限：哪类规范适合形式化（格式、结构），哪类不适合（学术语气的细微差别）
- GraphRAG 绑定机制的泛化性：是否可迁移到其他规范性课程

**展望**：
- KG 自动更新（新规范文档发布后的增量维护）
- 多语言规范支持（中英文写作规范的统一图谱）

---

## 电磁场案例的处理

降级为第4章末尾一节（4.7）或附录，定位为"平台跨课程可迁移性验证"，不再作为并列技术贡献展开。保留的内容：仿真工具调用接口设计（约0.5页），说明同一工具调用机制在不同课程中的复用。

---

## 关键工作量估计

| 任务 | 预估工作量 |
|------|----------|
| 本体设计（节点/关系类型定义）| 1周 |
| C路线：现有本体调研与映射 | 3-5天 |
| B路线：LLM抽取流程开发 + 人工审校 | 1-2周 |
| GraphRAG KG节点绑定实现 | 1周 |
| 评测数据集准备（标注） | 1周 |
| 用户实验（10-20人） | 1-2周 |
